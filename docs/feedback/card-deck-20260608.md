# Feedback: Card + Deck - 2026-06-08

## Type
[ ] Spec ambiguity   [ ] Suspected design error   [ ] Conflicting requirement
[x] Godot/runtime error   [ ] Proposed change   [ ] Other

## Context
- Task: docs/tasks/pending/TASK-001-card-deck.md
- Spec: docs/modules/card.md, docs/modules/deck.md
- Branch: feature/card-deck
- Files/lines: N/A - GUT runner/headless validation

## What's blocking / unclear
The Card and Deck tests pass, but the GUT 9.6.0 headless runner emits shutdown warnings/errors in Godot 4.6.3. This prevents the task from cleanly satisfying the "no errors/warnings" validation requirement even though the module tests pass.

The task's documented GUT command also needs a subdirectory flag for the requested `tests/unit/`
layout. Without `-ginclude_subdirs`, GUT reports "Nothing was run."

## Evidence (errors, conflicting lines, repro)
Command:

```text
godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -ginclude_subdirs -gno_error_tracking -gexit
```

Result summary:

```text
Scripts               2
Tests                 7
Passing Tests         7
Asserts              28
---- All tests passed! ----
WARNING: ObjectDB instances leaked at exit (run with --verbose for details).
ERROR: 3 resources still in use at exit (run with --verbose for details).
```

Without `-gno_error_tracking`, GUT also emits:

```text
ERROR: Condition "p_logger.is_null()" is true.
   at: add_logger (core/core_bind.cpp:708)
   GDScript backtrace:
       [0] register_logger (res://addons/gut/error_tracker.gd:14)
```

Task command as written:

```text
godot --headless --path . -s addons/gut/gut_cmdln.gd -gdir=res://tests -gexit
```

Observed result:

```text
[ERROR]: Nothing was run.
```

## My question for Opus
Should TASK-001 accept passing GUT assertions with `-ginclude_subdirs` plus this GUT/Godot headless shutdown warning noted, or should Sonnet adjust the GUT setup/version/config before Codex proceeds to PR?

---
### Resolution (filled by Opus)
- Decision:
- Spec updated: docs/modules/<module>.md section
- Date / by:
